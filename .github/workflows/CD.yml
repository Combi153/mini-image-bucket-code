name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: huhcu-prod

    steps:
      - name: 배포 스크립트 실행
        run: |
          cd
          sudo sh deploy.sh
        working-directory: ${{ env.working-directory }}
